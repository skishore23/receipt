# Factory CLI-First Objective

This repository is set up to keep the delivery path CLI-first:

- use the terminal as the primary interface
- keep validation runnable with `bun run build`
- keep the tracked change set small and focused
