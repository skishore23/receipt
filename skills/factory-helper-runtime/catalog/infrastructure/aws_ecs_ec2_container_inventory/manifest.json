{
  "id": "aws_ecs_ec2_container_inventory",
  "version": "1.0.0",
  "provider": "aws",
  "tags": [
    "ecs",
    "ec2",
    "containers",
    "inventory",
    "list",
    "table",
    "region",
    "tasks",
    "clusters"
  ],
  "description": "Inventory ECS tasks running on EC2 across queryable AWS regions and return container-level metadata.",
  "entrypoint": "run.py",
  "requiredArgs": [],
  "requiredContext": [
    "Uses the mounted AWS profile by default.",
    "Uses the queryable EC2 region scope when no explicit region scope is provided.",
    "Lists running ECS tasks, filters to launchType=EC2, and enriches them with container instance and EC2 metadata."
  ],
  "examples": [
    "--profile default",
    "--profile default --all-regions --output-dir .receipt/factory/evidence"
  ]
}
