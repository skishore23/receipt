{
  "id": "aws_s3_bucket_inventory",
  "version": "1.0.0",
  "provider": "aws",
  "tags": ["inventory", "s3", "buckets", "security", "encryption", "public-access", "acl", "policy"],
  "description": "Inventory S3 buckets with region, creation date, versioning, encryption, and public exposure signals.",
  "entrypoint": "run.py",
  "requiredArgs": [],
  "requiredContext": [
    "Uses the mounted AWS profile and default region unless overridden.",
    "S3 bucket listing is account-global; region is resolved per bucket for bucket-scoped calls."
  ],
  "examples": [
    "--profile default",
    "--profile default --output-dir /tmp/aws-s3-inventory"
  ]
}
